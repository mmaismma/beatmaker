const tileArray = document.getElementsByClassName('tile');
for (i = 0; i < tileArray.length; i++) {
    tileArray[i].addEventListener('click', play);
}
let a;
function play(event) {
    //let audioNumber = event.target;
    console.log('mmma');
    //console.log(audioNumber);
    //console.log(tileArray[x]);
    //console.log(x);
}